version: "2"
sql:
  - engine: "postgresql"
    queries: "internal/store/queries"
    schema: "db/migrations"
    gen:
      go:
        package: "store"
        out: "internal/store"
        sql_package: "pgx/v5"
        emit_interface: true
        emit_json_tags: true
        overrides:
          # pgvector support
          - db_type: "vector"
            go_type:
              import: "github.com/pgvector/pgvector-go"
              type: "Vector"
